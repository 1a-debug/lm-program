# Calculator Agent Demo

This deliberately small project is a reproducible target for demonstrating
the coding agent. One test initially fails because `divide` does not validate
a zero divisor. Ask the agent to inspect the project, run the tests, fix the
bug, and verify the result.
