# Known issue

The `divide` function does not handle a zero divisor and still raises the wrong
exception. This document is intentionally stale; current source code is the
authoritative implementation.
